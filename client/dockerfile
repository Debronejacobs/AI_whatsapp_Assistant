# Use a base image with Python
# Using a specific version like 3.9-slim is recommended for stability and smaller image size
FROM python:3.9-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the requirements file first to leverage Docker cache.
# If requirements.txt doesn't change, this step won't re-run on subsequent builds.
COPY client/requirements.txt ./

# Install dependencies using pip.
# --no-cache-dir is good practice in Dockerfiles to avoid storing package caches
# and reduce the final image size.
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code from the client directory into the container
COPY client/ ./

# Expose the port the FastAPI app runs on
EXPOSE 8001

# Command to run the application using uvicorn.
# Use 0.0.0.0 to bind to all network interfaces inside the container,
# making it accessible from outside the container via the mapped port.
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]